name: monitor-app
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - tk
  - numpy
  - matplotlib
  - pillow
  - pip:
      - customtkinter
      - pystray
      - bcrypt
      - pyinstaller
